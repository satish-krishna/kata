/target
**/*.rs.bk
node_modules/
dist/
.DS_Store
Thumbs.db
app/src-tauri/binaries/
scripts/build-release.log

# Release screenshots — regenerate with `npm run screenshots`, attach to the
# GitHub Release rather than committing (binary churn).
docs/screenshots/
