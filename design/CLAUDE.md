# Kata Workbench — design rules for Claude Code

The authoritative, up-to-date design rules live in **`app/CLAUDE.md`** (the dark "sumi-ink" system, single Hanada-azure accent, IBM Plex type, andon status palette, plus where tokens/components/fonts live and how to dev/review the app). Read that.

The full design system, per-component specs, and the Svelte mapping table are in `design/README.md`; pixel references are in `design/prototype/` (HTML/React — recreate in Svelte, never copy).
